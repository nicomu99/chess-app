package com.example.chess.model.moves;

import com.example.chess.model.Coordinates;

public class AttackMove extends Move {

    public AttackMove(Coordinates source, Coordinates destination) {
        super(source, destination);
    }
}
